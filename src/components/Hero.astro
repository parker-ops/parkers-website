---
---

<section class="relative min-h-screen flex items-center justify-center px-6 pt-20 pb-16 overflow-hidden">
  <!-- Subtle background accent -->
  <div class="absolute top-1/4 -right-32 w-96 h-96 bg-amber/5 rounded-full blur-3xl pointer-events-none"></div>
  <div class="absolute bottom-1/4 -left-32 w-72 h-72 bg-amber/3 rounded-full blur-3xl pointer-events-none"></div>

  <div class="max-w-4xl mx-auto text-center">
    <!-- Headshot -->
    <div class="animate-fade-up mb-8">
      <div class="relative inline-block">
        <img
          src="/assets/parker-headshot.jpg"
          alt="Parker Newell"
          class="w-32 h-32 sm:w-40 sm:h-40 rounded-full object-cover object-top border-3 border-amber/40 shadow-[0_0_40px_rgba(200,160,74,0.15)]"
        />
        <div class="absolute inset-0 rounded-full ring-1 ring-amber/20 ring-offset-2 ring-offset-midnight"></div>
      </div>
    </div>

    <!-- Eyebrow -->
    <p class="animate-fade-up delay-100 text-amber text-sm font-medium tracking-[0.25em] uppercase mb-6">
      Sales Leader &bull; Entrepreneur &bull; AI Advocate
    </p>

    <!-- Name -->
    <h1 class="animate-fade-up delay-200 font-heading text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-bold tracking-tight leading-[1.05]">
      Parker<br />
      <span class="text-amber">Newell</span>
    </h1>

    <!-- Tagline -->
    <p class="animate-fade-up delay-400 mt-8 text-lg sm:text-xl text-warm-gray max-w-2xl mx-auto leading-relaxed font-light">
      Co-Owner at <span class="text-warm-white font-medium">OneWave AI</span> — helping small and medium businesses
      harness the power of artificial intelligence to grow smarter, move faster, and cut the headaches.
    </p>

    <!-- Stats row -->
    <div class="animate-fade-up delay-500 mt-12 flex flex-wrap justify-center gap-8 sm:gap-12">
      <div class="text-center">
        <span class="block text-3xl sm:text-4xl font-heading font-bold text-amber">$3.2M+</span>
        <span class="text-xs text-warm-gray uppercase tracking-wider mt-1">Revenue Closed</span>
      </div>
      <div class="text-center">
        <span class="block text-3xl sm:text-4xl font-heading font-bold text-amber">7+</span>
        <span class="text-xs text-warm-gray uppercase tracking-wider mt-1">Years in Sales</span>
      </div>
      <div class="text-center">
        <span class="block text-3xl sm:text-4xl font-heading font-bold text-amber">40%</span>
        <span class="text-xs text-warm-gray uppercase tracking-wider mt-1">YoY Growth Driven</span>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="animate-fade-in delay-600 mt-16">
      <a href="#about" class="inline-flex flex-col items-center text-warm-gray hover:text-amber transition-colors group">
        <span class="text-xs tracking-widest uppercase mb-3">Scroll</span>
        <svg class="w-5 h-5 animate-float" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
        </svg>
      </a>
    </div>
  </div>
</section>
